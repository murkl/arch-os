# Build output — see /Makefile. Nothing under here is a source file: release/ is
# the installer as a machine runs it, dist/ is what a person downloads.
/release/
/dist/

# What a local run leaves beside itself, wherever it runs from. Both trees name
# their answers and their log after themselves — see runtime/main.go.
installer.conf
installer.conf.old
installer.log
installer.log.old
recovery.conf
recovery.conf.old
recovery.log
recovery.log.old

# Editors / OS noise
*.swp
.DS_Store
