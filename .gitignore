# Build output — see /Makefile. Nothing under here is a source file.
/release/

# What a local run of the installer leaves beside itself, wherever it runs from
installer.conf
installer.conf.old
installer.log
installer.log.old

# Editors / OS noise
*.swp
.DS_Store
