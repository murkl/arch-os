# The answers, put where another machine can pick them up — see share_config in
# lib.sh. Reached only by somebody who said yes to the offer above it: until that
# answer there is no upload, and this is a skipped row in the list.
#
# It guards itself, because the simulation inside share_config still answers with
# an address so the page that follows has something on it.

share_config
