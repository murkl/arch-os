title: Install Arch OS Manager
needs: [enable-multilib]
conditions: ARCH_OS_MANAGER_ENABLED == true

# Built from the AUR, which can be out of reach for days. Nothing else in the
# system needs it, and it can be installed from there once the AUR answers.
optional: true
