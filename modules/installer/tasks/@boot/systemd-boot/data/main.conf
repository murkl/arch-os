title   Arch OS
# The key the signed images carry out of os-release, so this system's entries
# sort together and ahead of the Recovery, which carries its own.
sort-key arch
linux   /vmlinuz-{{KERNEL}}
initrd  /initramfs-{{KERNEL}}.img
options {{CMDLINE}}
