title: Turn congestion notification off
needs: [pacstrap-core]

# Its own task rather than a branch inside configure-system: written down as a
# condition, the run lists this step only on the machines it is for.
conditions: ARCH_OS_ECN_ENABLED == false
