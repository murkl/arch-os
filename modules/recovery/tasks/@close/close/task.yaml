title: Unmount and lock the disk
confirm: |
  Unmount the system and lock the disk again?

  Nothing is lost either way, the recovery is finished. Leaving it mounted
  is only useful if you want to keep working on it from here.

# Everything the recovery mounted, taken back down, and the encrypted volume
# locked again.
script: |
  simulating && return 0

  close_target
