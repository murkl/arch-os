# Can this machine write a bootable device at all? Every check in this stage is
# read-only and every one of them is a hard stop, run before the first question
# because none of it can be fixed from inside this program.
#
# Whether the image can be got at is deliberately not here: where it is kept is
# an answer, and this stage runs before there is one. The download task says so
# instead, and says it about the folder that was actually chosen.
title: Root for the write
