# No login on any other console either: the Recovery on tty1 is the only way
# into this machine. tty1 itself is taken by its unit, which conflicts with the
# getty there and, started again whatever becomes of it, never hands tty1 back.
[Login]
NAutoVTs=0
ReserveVT=0
