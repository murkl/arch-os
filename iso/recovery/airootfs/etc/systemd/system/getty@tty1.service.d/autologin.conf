# Whatever becomes of the Recovery, tty1 is handed back as a root prompt: the
# systemd unit that starts it hands over to this getty, as it does on the ISO.
[Service]
ExecStart=
ExecStart=-/usr/bin/agetty --noreset --noclear --autologin root - ${TERM}
