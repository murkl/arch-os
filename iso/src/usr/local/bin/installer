#!/usr/bin/env bash
# Straight into the installer, past the question of which program to open. The
# same run `arch-os` starts, with the answer already given.
set -eu

exec arch-os -dir installer "$@"
