#!/usr/bin/env bash
# Straight into the recovery, past the question of which program to open. The
# same runtime and the same folder as the installer; -dir is what says which of
# the two trees beside it this is.
set -eu

exec arch-os -dir recovery "$@"
